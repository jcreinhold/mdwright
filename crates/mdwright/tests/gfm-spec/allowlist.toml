# GFM spec editorial-deviation index.
#
# Each entry is a per-case TOML record documenting why mdwright's
# formatter intentionally diverges from the spec example. Entries here
# are *not* tracked as regressions in `snapshot.txt`; they are choices.
#
# Required fields per entry:
#   number — spec example number (1-based, as parsed by gfm_spec.rs)
#   bucket — short tag grouping similar deviations
#   reason — one-line justification
#   docs   — pointer to authoritative explanation
#
# Keep entries short. Adding here moves a failure out of `snapshot.txt`;
# the snapshot test asserts every non-allowlisted failure is recorded.
#
# Current buckets:
#   pulldown-text-chunking — AST-only mismatches that reflect
#     pulldown-cmark's text-run chunking, not a formatter deviation.
#     HTML matches; output is byte-equivalent under cmark-gfm. See
#     `docs/deviations.md#pulldown-text-chunking-deviations` for the
#     shared rationale and the triage rule.

[[case]]
number = 5
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 6
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 7
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 16
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 19
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 61
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 102
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 103
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 214
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 230
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 232
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 242
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 248
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 249
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 251
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 252
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 256
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 264
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 265
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 266
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 268
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 320
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 321
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 324
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 330
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 333
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 393
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 411
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 499
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 500
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 503
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 520
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 528
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 536
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"

[[case]]
number = 640
bucket = "pulldown-text-chunking"
reason = "AST-only mismatch: pulldown-cmark's text-run chunking differs from cmark-gfm; HTML matches."
docs = "docs/deviations.md#pulldown-text-chunking-deviations"
