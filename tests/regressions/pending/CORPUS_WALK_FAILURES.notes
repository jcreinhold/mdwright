<!-- proptest::corpus_walk shrunk 2026-05-14 -->
<!-- PENDING: deferred to session 13b.

     `MDWRIGHT_CORPUS_TEST=1 cargo test --release -p mdwright
     corpus_walk` reports ~11 non-idempotent files in
     `docs/books/gentle-sga/i/`. At least one root cause is
     distinct from the list-marker bug:

       * **Indented code block inside list item**: each
         format pass adds two spaces of leading whitespace to
         the code block's content lines. Reproducer (first
         failing file, line 80):

         ```
         - For each pair of opens `V ⊆ U`, a ring map

           ```text
               ρ_{V, U} : ℱ(U) → ℱ(V)
           ```
         ```

         Once: 6 spaces before `ρ`. Twice: 8 spaces. The
         indent budget for the fenced code block's interior is
         being added to the source indent on every emit.

     Other failing files likely surface additional bugs; this
     note is the entry point, not an exhaustive catalogue.
-->
