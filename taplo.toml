exclude = ["**/target/**/*.toml"]
include = ["*.toml", "crates/**/*.toml", "docs/**/*.toml", "fuzz/Cargo.toml", "xtask/Cargo.toml"]

[formatting]
column_width = 120
reorder_arrays = true
reorder_keys = true
